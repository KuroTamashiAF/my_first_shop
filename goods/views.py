from django.shortcuts import render

# Create your views here.
def catalog(request):
    "контроллер каталога"
    return render()


def product(request):
    "контроллер продукта" 
    return render()